# Handover Notes (2025-11-01)

## VBA / Dashboard
- `excel/VBA/AutoTrader.bas`
  - `FindColumn` が日本語ヘッダにも対応するようエイリアスを拡張し、`FindATRColumn` を追加（`ATR`/`ATR_n` どちらでも参照可能）。
  - `PushCandidatesToDashboard` で AT〜AW（Entry/TP/SL/Status 列）を `ClearContents` の対象から外し、候補再読み込み時に数式が失われないよう修正。完了後に `ApplyRealtimeColumns` と `ApplyDashboardHeaderLabels` を呼び出し、数式と日本語ヘッダを再配布。
  - `ApplyDashboardHeaderLabels` を新設し、シート保護を一時解除してから日本語ヘッダを設定→再保護する流れを自動化。
- `scripts/restore_dashboard_formulas.py` を実行して NewDashboard I〜T 列の RSS 系式を再配布済み。

## フィルタ & ユニバース
- `tools/aggregate_candidates_today.py` の既定閾値を `WinCI_L ≥ 0.65`, `PF ≥ 1.20`, `ForwardTrades ≥ 3` に緩和（`dd_scale=1000`）。`python tools/aggregate_candidates_today.py` 実行済み（現在 20 銘柄）。
- `tools/simulate_expected_pnl.py` で新/旧セットを比較。予算 1,000 万円 等分配想定で **+34,915 円/日**（旧構成との差分 +6,921 円）。
- ユニバースは Top300 に拡大。`scripts/nightly_build.cmd` / `run_nightly_now*.cmd` / `run_weekend_fullscan.cmd` の `--universe-size` を 300 に変更し、`scripts/run_fast_nightly.ps1` の既定 `UniverseFile` も `universe_amt_top_300_*.csv` に更新。`TopK` を 20 へ増加。
- `scripts/run_fast_nightly.ps1` の coarse 解析パラメータを緩和（`--min-train-trades 8`, `--min-forward-trades 2`, `--forward-pf-min 1.0`, `--mask-threshold 1.10`）。
- テスト実行：`output/bt30/NIGHTLY_20251101_FAST_TEST/RUN_coarse_AM0930_j-only/_TOP_CANDIDATES.csv` は **2 銘柄** に増加（従来 1 銘柄）。

## 状態
- Universe 拡大と閾値緩和は次回 nightly/FAST 実行から有効（既存成果物は旧設定）。
- Excel は閉じられており、`SHINSOKU_backup_20251101_*.xlsm` を取得済み。差し替えは `excel_install_macros.py` など通常手順で実施可能。

# Handover Notes (2025-10-29 PM)

## AutoTrader 待ち伏せロジック強化
- AutoTrader.bas に HandlePendingStraddle / HandleActiveStraddle を追加し、30秒周期でエントリー指値と TP/SL の差し替えを実行するようにしました。
  - B19 (Entry Grace Minutes) の猶予後に差替え開始、B20 (OCO Update Seconds) を周期として使用。
  - 既存注文との差が約1ティック以上になった場合のみ再発注し、過剰なキャンセルを防止。
  - 約定後は ctiveStraddles 辞書に最新価格と更新時刻を保存し、TP/SL の再発注を管理。
- EvaluateAndQueueOrders の先頭で MaintainStraddleOrders を呼び出し、セッション外でも保守が走るようにしました。
- GetTickSize 直下に AlignToTick を追加し、再計算した価格を取引所ティックに丸めています。

## サンプル20銘柄テスト (NIGHTLY_20251029_test)
- コマンド: python tools/analyze_param_stats.py --root output/bt30_test/NIGHTLY_20251029 --out reports/param_stats/NIGHTLY_20251029_test
- 主な結果 (trades>=500で集計):
  - J=1.20 : 期待値 +10.6bp / 勝率 59.1% / PF 1.77 / 7,056トレード
  - J=1.10 : 期待値 +10.3bp / 勝率 58.0% / PF 1.65 / 6,984トレード
  - J=1.30 : 期待値 +8.5bp / 勝率 60.1% / PF 1.84 / 6,711トレード
- eports/param_stats/NIGHTLY_20251029_test/ に J別テーブルとヒートマップCSVを配置済み。
- 低J帯 (<=0.8) は勝率48%以下・PF<1で依然として低調。マスク候補に維持。

## TODO / 次フェーズ
1. Nikkei連動・ギャップ連動など仮説1～4 + H1～H3 のパラメトリック検証を順次実装。
   - まず既存パイプラインに日経平均1分足・ギャップ情報を流す処理を追加（data/mkt 系）。
2. 週末フルバッチで抽出した無効帯 (state/ineffective_bands.json) を平日の --mask-ineffective に渡す自動化を継続監視。
3. Queue Now ボタン経由のドライランで新待ち伏せロジックを数銘柄テストし、Orders/PnL のログを確認。
4. Docs: 仮説検証の結果をまとめるテンプレを docs/hypothesis_log.md として用意予定。

## 備考
- 300銘柄本番バッチは未実行。テストユニバース20銘柄のみ。
- Optuna/TPE + ASHA の依存関係 (optuna) は環境に導入済み。--enable-bayes は optuna が import 可能な場合のみ動作。

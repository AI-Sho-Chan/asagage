# Handover 2025-12-18

## 1) 今日起きていたこと（結論）
- Excel（`ASAGAKE.xlsm`）の DEMO 動作自体は動いていて、Orders には「約定扱い→利確/損切り」までのログが残っていました。
- “全部キャンセルに見える”主因は、候補CSVの中に **列が欠けた古い形式の行が混ざって**しまい、TP/SL などが空のまま入り、見た目が不自然になっていたことです。
- 対応として、`candidates_nextday.csv` を「混在なしの最新1セット」に作り直せるようにし、さらに TP/SL が空にならないように補完しました。

## 2) 解析した Excel スナップショット（DEMOログの実態）
- 解析ファイル: `C:\AI\asagake\ASAGAKE_live_snapshot_20251218_150006.xlsm`
- 状態: `RunStatusV2=DEMO_RUNNING`
- 取り込み行数: 444行 / 31銘柄
- Orders（集計・概略）
  - `preplace_demo / ORDERED`: 937
  - `preplace_demo / CANCELLED_AUTO`: 1574
  - `preplace_demo / RUNNING`: 13
  - `preplace_demo / CLOSED`: 30
  - `sl_demo / FILLED`: 30
  - `tp_demo / ORDERED`: 13
- 重要ポイント
  - `CANCELLED_AUTO` が多いのは「先回り指値を出し直す」性質があるためで、異常ではありません。
  - `RUNNING / CLOSED / sl_demo` が存在するので、約定→決済の流れは発生しています。
  - ただし 444行のうち多数が `TP_per_J_row / SL_per_J_row` 空（=0扱い）になっており、これが“不自然”の原因でした。

## 3) 「混在なしの最新1セット」とは（誤解が起きやすい点）
- 「同じ銘柄に複数プランを採用する」こと自体はOK（仕様どおり）。
- 問題だったのは、**プランの複数採用ではなく**、候補CSVの中に「古い形式の列欠け行」などが混ざっていたことです。

## 4) 修正（Windows / ローカル）
- `tools/aggregate_candidates_today.py`
  - `candidates_nextday.csv` に `TP_per_J_row / SL_per_J_row / Trail_per_J_row / GapBanPct / NoTradeMin / BudgetFactor_row / live_demo_class / NKY_AllowedSide / TOPIX_AllowedSide` を出力。
  - 元データで空なら `TPk/SLk` から補完して「空→0扱い」を防止。
  - `--date-tag` で特定日だけを集計できる（=混在を防げる）。
- `scripts/run_nightly_candidates.ps1`
  - `nightly_build_candidates.py` が早期終了しても、必ず `aggregate_candidates_today.py` を走らせて `candidates_nextday.csv` を更新するように変更。
  - `--excel` は `ASAGAKE.xlsm` を参照（旧SHINSOKU系を避ける）。
- タスクスケジューラ
  - `Asagake Nightly FAST`: 月〜木 16:30（※金曜はVM週末バッチ運用のため除外）

## 5) 18:00 DailyReplay（仮想売買）について
- 2025-12-18 18:00 の自動実行は一度失敗しました（Yahooの1分足がまだ揃っていないタイミングがあり得ます）。
- 対策: `scripts/run_daily_replay.ps1` にリトライを実装し、失敗しても「少し待ってから再試行」してメールまで送れるようにしました。
- さらに、メール本文が読めるように `tools/simulate_daily_replay.py` が `analysis/daily_replay_YYYYMMDD_mail.txt`（日本語の説明付き）を生成し、`run_daily_replay.ps1` はそれを送信します（メール送信時の文字コードは UTF-8）。
- 参考: `C:\AI\asagake\logs\daily_replay_task_YYYYMMDD.log` に結果が残ります。
  - 例（12/18）: 18:00 は失敗 → その後の再実行で `analysis/daily_trades_20251218.csv` と `analysis/daily_replay_20251218.json` を生成し、メール送信まで完了。

## 6) VM 側（週末バッチが走らなかった件の原因）
- 12/12 16:30 の cron で `run_weekend_only.sh` が CRLF（Windows改行）混入のため即死していました。
- 対策: cron は repo 内の `vm_run_weekend_only.sh` を `bash ...` で実行する形に寄せ、shebang問題が起きにくい形にしました。

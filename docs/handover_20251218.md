# Handover 2025-12-18

## 1) 今日やったこと（結論）

- **Excel（ASAGAKE.xlsm）のDEMO動作**について、保存済みファイル `C:\AI\asagake\ASAGAKE_live_snapshot_20251218_150006.xlsm` を解析し、**「動いてはいる（約定扱い→決済ログは出ている）」**ことを確認。
- ただし、候補CSVが「古い候補が混ざった状態」でImportされると、**無効な行（TP/SL設定が空）だらけになり、キャンセルや0損益決済が増える**ことを確認。
- その再発防止として、**ナイトバッチが「最適化対象なし」で早期終了しても、必ず `candidates_nextday.csv` を作り直す**ように修正。
- さらに、`candidates_nextday.csv` に **`TP_per_J_row / SL_per_J_row / Trail_per_J_row` が無い（または空）**場合でも、`TPk/SLk` から補完して出力するように修正。

## 2) Excel（DEMO）の検証結果（2025-12-18 保存分）

対象ファイル:
- `C:\AI\asagake\ASAGAKE_live_snapshot_20251218_150006.xlsm`

確認できた状態:
- `RunStatusV2=DEMO_RUNNING`
- ダッシュボード取込行数: **444行（31銘柄）**
- Orders 末尾行: **2628行**

Orders の内訳（件数）:
- `preplace_demo / ORDERED`: **937**
- `preplace_demo / CANCELLED_AUTO`: **1574**
- `preplace_demo / RUNNING`: **13**
- `preplace_demo / CLOSED`: **30**
- `tp_demo / ORDERED`: **13**
- `tp_demo / CANCELLED_AUTO`: **30**
- `sl_demo / FILLED`: **30**

重要ポイント（ユーザーの見え方の説明）:
- `CANCELLED_AUTO` が大量に見えるのは、**「板やVWAP/Jの更新で、置き直しが発生している」**ためで、必ずしも「何も起きていない」ではありません。
- 実際に **`RUNNING` や `CLOSED` が出ており、約定扱い→決済（損切り）が実行されたログが残っています**。

追加の発見（候補CSVの混在が原因）:
- 444行のうち **約299行は `TP_per_J_row/SL_per_J_row` が空（=0扱い）**になっており、これが
  - TP/SL が 0 に近い（＝すぐ決済扱いになったり、動きが不自然に見える）
  - 置き直し・キャンセルが増える
  の原因になりやすい。

## 3) ナイトバッチ（Windows）状況

- タスク: `Asagake Nightly FAST`（**MON〜THU 16:30**）
- 直近: 2025-12-18 15:39 の手動スモークでは `nightly_build_candidates.py` は **「劣化銘柄なし」で早期終了**。
- ただしこれまでの挙動だと、早期終了時に `candidates_nextday.csv` が更新されず、古い候補が残ってしまうことがあったため、今回 **必ず `tools/aggregate_candidates_today.py` を実行して更新する**ようにした。

## 4) 週末VM（12/12 16:30 に走らなかった原因）

結論:
- 12/12 の cron 実行ログ `~/cloud_logs/cron_stdout.log` に
  - `/home/shouichi_ikeda/run_weekend_only.sh: 1: ﻿#!/bin/bash\r: not found`
  - `set: Illegal option -o pipefail`
  が残っており、**`run_weekend_only.sh` が Windows の改行（CRLF）＋BOM 混入で壊れていて、起動直後に落ちていました。**

対策:
- cron は現在 `bash /home/shouichi_ikeda/asagage/vm_run_weekend_only.sh ...` を呼ぶ設定になっており、**Windows経由の scp ファイルに依存しない**ため、次の金曜はこの原因では止まりません。
- 念のため `~/run_weekend_only.sh` も `~/asagage/vm_run_weekend_only.sh` からコピーし直し（LF/正しいshebang）済み。

## 5) 実装差分（このコミットに入れる）

- `scripts/run_nightly_candidates.ps1`
  - `--excel` を `ASAGAKE.xlsm` に変更
  - nightly が早期終了でも `tools/aggregate_candidates_today.py` を実行して `candidates_nextday.csv` を更新
- `tools/aggregate_candidates_today.py`
  - `TP_per_J_row / SL_per_J_row / Trail_per_J_row` が無い/空の場合に `TPk/SLk` から補完


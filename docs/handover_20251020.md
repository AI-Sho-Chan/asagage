
# Update 2025-10-25

- Restored `SHINSOKU.xlsm` from backup `SHINSOKU_backup_20251024_061734.xlsm`.
- Restored `AutoTrader.bas` from `AutoTrader.bas.bak`.
- Editing policy: never open Excel/VBA for direct changes. Prepare a copy, modify XML or .bas offline, then replace the workbook.
- Use `python scripts/repair_realtime_formulas.py` to re-flow real-time formulas (I6/J6/K6/N6...).
- Always create a timestamped backup (e.g. `SHINSOKU_YYYYMMDD_HHMMSS.xlsm`) before any workbook edits.

## Update 2025-10-25 (午後)

- `AutoTrader.bas` にヘッドレス実行時のログ出力／エラーハンドリングを追加し、`SetColumnFormula` での書式エラーをデバッグしやすくした。
- `PushCandidates` 実行時に保護エラーが発生しても処理を継続し、エラー内容を `logs/autotrader_debug.log` に記録するよう変更。
- 現行の Excel では `SetColumnFormula` が保護シートに対して RSS 関数を貼り付ける際に `Run-time error 1004` となるため、`PushCandidates` 直後は I/J/K/N… 列の式が欠落し `#VALUE!` がそのままコピーされる。  
  → 対策として、**CSV を読み込む前または直後に必ず `python scripts/repair_realtime_formulas.py` を実行**し、対象列の式を COM 経由で再適用すること。  
  → `repair_realtime_formulas.py` はシート保護を解除してから R1C1 形式で式を流し込み、再保護するため、I6 以降の式が確実に復元される。
- 当面の復旧手順：  
  1. `python scripts/repair_realtime_formulas.py` を実行してダッシュボードの式を再配置。  
  2. `python scripts/run_autotrader_macros.py`（または手動で Reset → Load → Push）を実行。  
  3. ログ `logs/autotrader_debug.log` に `SetColumnFormula error` が記録されても、上記スクリプトで式が復元されていることを確認し、必要に応じて再度 repair スクリプトを走らせる。
- 現在の途中完成版として `SHINSOKU_partial_20251025_135952.xlsm` を作成し、ロールバック用に保存済み。


# Handover 2025-12-22

## 1) DailyReplay の改善（「なぜ負けた/勝ったか」を見える化）
- `tools/simulate_daily_replay.py` の出力 `analysis/daily_trades_YYYYMMDD.csv` に、決済理由を追加しました:
  - `exit_reason`: `TP`（利確）/ `SL`（損切り）/ `EOD`（引け決済）
  - `SL_SAME_BAR`: 1分足の同じ足の中で「利確にも損切りにも触れた」可能性があるため、保守的に損切り扱い
  - 参考値として `tp_px` / `sl_px` / `tp_dist_bp` / `sl_dist_bp` も追加
- 毎日 18:00 のメール本文に、次を追加しました:
  - 決済理由（内訳）
  - 「負けが大きかった順（上位5件）」に `exit_reason`（どう負けたか）と `bars`（何分で決済されたか）を併記

## 2) 現時点の学び（次にデータが増えたらやること）
- 5801.T のような「戻らない（負けが続きやすい）タイプ」は、将来データが溜まった段階で
  - `DEMO_ONLY` に落とす / 予算（BudgetFactor）を下げる / 守り条件を強める
  を検討する。
- 8105.T のような「急な動きで一気に損切りになりやすいタイプ」は
  - `GapBanPct`（寄り付きの急なギャップ回避）
  - `NoTradeMin`（寄り付き直後の荒い時間を避ける）
  の調整対象として、DailyReplay で検証する。

## 3) 補足（1分足の限界）
- 1分足では「1分の中で、利確と損切りのどちらが先に触れたか」が分からないことがあります。
- その場合は、安全側（損切り扱い）に寄せています（`SL_SAME_BAR`）。

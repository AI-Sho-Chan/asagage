# 引継ぎメモ 2026-01-31

## 1) 週末バッチ（週次候補生成）の実行確認（ローカル生成物ベース）

※VM 側の `cloud_logs/weekend_YYYYMMDD_*.log` まで突合できると「VMで完走」の確証になるが、まずは Windows 側に同期されている生成物で「実行された」ことを確認した。

- 2026-01-23（金）週の候補：
  - `output/excel/candidates_for_20260123.csv` が存在（更新: 2026-01-24 20:09 JST 相当）
- 2026-01-30（金）週の候補：
  - `output/excel/candidates_for_20260130.csv` が存在（更新: 2026-01-30 21:13 JST 相当）
  - `output/excel/candidates_nextday.csv` が存在（更新: 2026-01-30 21:13 JST 相当）
  - `output/bt30/NIGHTLY_20260123/` と `output/bt30/NIGHTLY_20260130/` が存在

## 2) 週末バッチ候補の差分（2026-01-23 vs 2026-01-30）

- 主要キー（`Ticker, session, SignalMode, ATR_n, TPk, SLk, J_th, TMAX`）で比較して **27行が完全一致**。
- セッション分布も一致：
  - `AM0930(10), AM0945(5), AM15(4), AM1015(3), AM1030(2), MID1030(2), PM1230(1)`
- `live_demo_class` 分布も一致：
  - `LIVE_STRONG=6 / LIVE_BASE=12 / DEMO_ONLY=9`

解釈（暫定）：
- Top200の顔ぶれや、WF（60日窓）での「勝ち残り条件」が週を跨いでも大きく変わらず、結果として同じ候補に落ちた可能性が高い。
- 週末バッチが「毎週必ず違う候補を出す」設計ではなく、「条件を満たすものが変わらなければ同じになる」設計なので、これは仕様として起こり得る。

## 3) DailyReplay（18:00 仮想売買）中間集計（2025-12-08〜2026-01-30）

ファイル：`analysis/daily_replay_history_summary.csv`

- 対象日数：27日（取引あり21日 / 取引なし6日）
- 総取引数：202
- 総損益：-1,677,741.5 円

クラス別（累計）：
- `LIVE_STRONG`: 51回 / +1,414,522.7 円
- `LIVE_BASE`:   96回 / -2,727,764.3 円
- `DEMO_ONLY`:   55回 / -364,499.9 円

示唆：
- 「Strongはプラス、Base/Demoが大きくマイナス」なので、**日次レポートは Strong を主役**にし、Base/Demoは「損失監視の参考枠」に落とす方針が妥当。

## 4) Excel（DEMO）Ordersログの監査（読み取り）

監査出力：`analysis/excel_audit/orders_usedrange_20260131_104004.csv`

主要な観測：
- `status` に `MODIFIED` がほぼ見当たらない一方、`CANCELLED_AUTO` が多い日がある。
  - → 指値更新は「指値変更（Modify）」ではなく、**取消→再発注**で行っている可能性が高い。
- `note` に `DEMO_EOD_FLAT / DEMO_TIMEOUT / DEMO_SL / DEMO_TP` 等が多く、DEMOは「EODで必ずフラット化」「一定時間でタイムアウト決済」などの安全系のロジックが多数動いている。

## 5) DEMOの場中停止（持ち越しゼロ）設定

ファイル：`state/strategy_rules.ini`

- `demo_soft_flat_time=15:15`
- `demo_flat_time=15:25`
- `demo_stop_time=15:35`
- `demo_max_hold_min=30`

目的：
- 翌営業日への持ち越し（RUNNING残）を極力ゼロに寄せる。

## 6) 「Import Candidates が 1件だけ」になった件（過去ログ）

`logs/vba_events.log` にて、2026-01-14〜01-19 に `imported=1` が複数回発生。
その後（2026-01-23 以降）は `imported=27` が継続。

暫定推測：
- その時点の `output/excel/candidates_nextday.csv` が実際に1行だった、または Import側のフィルタ条件で実質1行になった可能性が高い。
- 現状は再現していないため、再発時は必ず「当時の candidates_nextday を別名で退避」して原因特定する（例：`candidates_nextday_YYYYMMDD_HHMMSS.csv`）。


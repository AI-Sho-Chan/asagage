# 引継ぎメモ（2026-01-31）

## 1. 週末バッチ（VM）実行確認：2026/01/23・2026/01/30

ローカル `C:\AI\asagake` 側に同期されている成果物から、週末バッチが少なくとも「候補CSVを生成するところまで」実行されていることを確認。

- `output/excel/candidates_for_20260123.csv`（27行 / 19銘柄）
- `output/excel/candidates_for_20260130.csv`（27行 / 19銘柄）
- `output/excel/candidates_nextday.csv`（27行 / 19銘柄、2026/01/30 の更新）

上記2日（2026/01/23 と 2026/01/30）の候補は、以下の比較キーで **27/27 が完全一致**：
- `Ticker, session, SignalMode, ATR_n, TPk, SLk, J_th, TMAX`

差が出ていたのは「参考指標」：
- `CorrNKY` / `CorrTOPIX` は 27行中 24行で差分あり（値は更新されるが、採否・設定自体は同じ）

※「候補が同一だった」ことは「バッチが壊れている」ではなく、評価窓60日で見たときに上位候補の順位が動かなかった可能性がある（市場が似た状態／評価が安定／差分が小さい等）。

## 2. DailyReplay（平日18:00仮想売買）の全期間ざっくり集計（直近の作業時点）

`analysis/daily_replay_history_summary.csv` を集計（27日分）。

- 総トレード：202
- 総損益：-1,677,741.5 円

クラス別：
- `LIVE_STRONG`: 51回 / +1,414,522.7円（平均 +27,736円/回）
- `LIVE_BASE`: 96回 / -2,727,764.3円（平均 -28,414円/回）
- `DEMO_ONLY`: 55回 / -364,499.9円（平均 -6,627円/回）

運用方針（合意）：
- メール本文の主役は `LIVE_STRONG`
- `LIVE_BASE` / `DEMO_ONLY` は「別枠（損失監視・原因調査用）」として小さく載せる

## 3. Excel(DEMO) Ordersログ（場中ログ）の状況（監査CSVから）

監査用CSV例：
- `output/orders/orders_20260131_095335_audit.csv`

直近7営業日（2026/01/19,21,22,23,26,27,28）の「場中（09:00〜15:30）」に絞った集計：
- 行数：1436
- status内訳：CLOSED 1406 / CANCELLED_AUTO 19 / FILLED 11
- note内訳（上位）：DEMO_EOD_FLAT, DEMO_TIMEOUT が大半

※「場中の注文→約定→決済」が動いている形跡はある一方で、
`DEMO_EOD_FLAT/DEMO_TIMEOUT` が多く、ログが「場中以外の実行」や「古いデータ混入」などで汚れている可能性がある。
以後の分析は「場中だけ」「直近日だけ」「異常値（価格等）除外」で切り出して行うのが前提。

## 4. DEMOの場中停止・当日クローズ（VBA仕様の現状）

`excel/AutoTraderAdvanced.bas`（抜粋）：
- DEMOは「JPX現物時間（09:00-11:30, 12:30-15:30）」以外は、注文生成/処理をしない
- DEMOは `demo_stop_time`（既定 15:35）以降に `StopAutoTickV2` を呼び、`RunStatusV2=IDLE` に戻す
- DEMOの当日クローズ（引け前決済）ルール：
  - `demo_soft_flat_time`（既定 15:15）：含み損でなければ寄せる（DEMO_EOD_SOFT）
  - `demo_flat_time`（既定 15:25）：残りは強制決済（DEMO_EOD_FLAT）

## 5. Importで「1銘柄しか入らない」現象について（再発防止の観点）

2026/01/09 は `output/excel/candidates_for_20260109.csv` が 1行（=候補が1行しかない状態）であった。
Importが「1銘柄/1プラン」になる原因は、Excel側の不具合以外にも「候補CSVが実際に1行」や「候補CSVが更新されていない」場合があり得る。

再発時の一次切り分け：
- `output/excel/candidates_nextday.csv` の行数確認（ヘッダ除き2行目以降が何行あるか）
- `output/excel/candidates_for_YYYYMMDD.csv` の更新日時確認（週末バッチ同期ができているか）


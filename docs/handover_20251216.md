# Handover 2025-12-16

## 1) 今回やったこと（結論から）
- `tools/simulate_daily_replay.py` に「比較検証の前提ルール」を反映しました。
  - 同じ銘柄は同時に1ポジションだけ
  - 決済後のみ再エントリー（クールダウン5分）
  - 1日あたり同一銘柄は最大2回まで
- これにより、同じ銘柄・同じ時刻に「複数プランが同時に約定した扱い」になっていた問題が解消し、より現実に近い集計になりました。

## 2) 12/12 と 12/15 のDailyReplay（修正後）結果
### 12/12（候補: `output/excel/candidates_20251212_M3.csv`）
- 取引数: 10
- 損益合計: 約 -373,833 円
- 大きな負けの中心: `6525.T`（-124,346円）, `8316.T`（-107,971円）

### 12/15（月曜・候補: `output/excel/candidates_nextday.csv`）
- 取引数: 6
- 損益合計: 約 +274,955 円
- 大きな勝ちの中心: `6740.T`（+319,391円）
- 大きな負けの中心: `8411.T`（-62,722円）

※「修正前」は同一銘柄が同時刻に複数回カウントされ、12/15 が過大に良く見えていました（修正後は抑制されています）。

## 3) ラーニング（わかったこと）
- 同じ候補セットでも「日によって勝ち負けが大きく変わる」。
  - 12/12 は“戻らない動き”が混ざり、逆張りがやられやすい日だった可能性が高い。
  - 12/15 は“戻りやすい動き”が多く、特定銘柄（`6740.T`）が大きく寄与した。
- 1日単発の勝ち負けで判断するとブレるので、**同じルールで複数日（最低でも20営業日）**のDailyReplayで評価する必要がある。

## 4) 次アクション（おすすめ順）
- ① DailyReplay を「直近20営業日」で回し、M0/M3 のどちらが安定して良いかを数字で確かめる（同条件で比較）。
- ② 「負けが大きい銘柄・パターン」を棚卸しして、候補の段階で弾ける条件がないか検討（例: GapBan/NoTradeMin の見直し）。
- ③ 週末バッチの“時間帯分割（M3）”自体は候補の期待値が高い傾向だが、**最終判断はDailyReplay（実損益に近い）で**行う。


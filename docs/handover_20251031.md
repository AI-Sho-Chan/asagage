# Handover Notes (2025-10-31)

## 概要
- ナイトバッチ: FAST構成（Top150、ASHA + Bayes短縮、H1/H3有効化）で AM0930 2プラン完了、AM0945 j-only 粗完了・リファイン0件、AM0945 j-cross 粗→リファイン完了。
- 候補CSV: `output/excel/candidates_20251030.csv`（AM0930系）、`output/excel/candidates_20251031.csv`（AM0945 j-cross）を生成。
- ギャップ時J調整/市場連動J調整/動的TP・SL（|J|-J_th連動）をバックテスト側で有効化し検証。
- スケジュール: 16:30 スタート用のFASTタスクスクリプトを追加（登録スクリプト付）。05:30朝バッチは日次前処理で継続。

## 主要変更
- scripts/bt_opt30_forward.py: H1/H3のパラメタをCLIで追加、無効域マスクで高J温存、評価ロジックに市場連動Jと動的TP/SLを実装。
- scripts/nightly_build_candidates.py: 粗/リファインにH1/H3フラグと高J温存パラメタを伝播。
- scripts/run_fast_nightly.ps1: Top150×4プランをFAST設定で一括実行。
- scripts/run_fast_am0945.ps1: AM0945 2プランのみの実行ユーティリティ。

## 実行結果（ダイジェスト）
- AM0930 j-cross: 4銘柄リファイン、Bayes 56試行、83.4分、候補CSV出力。
- AM0930 j-only: 粗完了→リファイン0銘柄。
- AM0945 j-only: 粗20.4分→リファイン0銘柄。
- AM0945 j-cross: 粗9.9分、リファイン56.9分（上位1銘柄）、候補CSV出力。

## 未完了/積み残し
- NewDashboardへの動的TP/SL（VBA）実装の反映（バックテスト係数の最終化後、VBAへ移植）。
- ナイトバッチのスケジュール登録を16:30へ更新（管理者権限でTask Scheduler登録要）。
- 候補の最終統合CSV `output/excel/candidates_nextday.csv` の集約・重複排除（本日完了予定）。
- パラメタ閾値（市場ΔJ、ギャップ帯J加算、TP/SL係数）の継続的最適化。

## 連絡事項
- 05:30朝バッチはデータ鮮度・ユニバース更新に必要で、ナイト未完でも有効。
- SHINSOKU.xlsm は必ずバックアップ後にCOM経由で更新（システム概要タブ更新スクリプト同梱予定）。

## 追加変更（市場連動J_th×ボラ、低J排除、単一採択）
- 最小J_thを0.8へ引上げ（0.6を常用から外す）。夜間パイプライン・グリッドに適用。
- 銘柄ごとに1組合せだけを採択するスコア（pf×win^1.2×log1p(trades)/(1+DD/1000)）を導入。WinCI_L≥0.70、PF≥1.30、Trades≥5で事前フィルタ。
- ギャップ視覚化（GapDecision列: OK/STOP_GAP/SKIP_OPP）を追加。停止時は発注ガード一致。
- H3の実効係数（TPk_eff/SLk_eff）を列表示（TP/SL価格と併せて可視化）。
- 市場ボラ連動のΔJを追加（B32:阈値bp、B33:上振れΔJ/10bp、B34:下振れΔJ/10bp）。BuildAdjustedJExpressionに反映。
- SystemOverview/ダッシュでMarket Biasを自動算出しB21へ注入（指数5分bp）。

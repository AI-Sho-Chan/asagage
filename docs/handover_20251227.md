# 2025-12-27 Handover

## Laptop handoff (full clone)
- Full clone completed on laptop.
- `ASAGAKE.xlsm` and `state/smtp.json` are **not tracked in Git** (`.gitignore` excludes `*.xlsm` and `state/smtp*.json`), so they must be copied manually.
- Minimum files to copy from desktop to laptop:
  - `C:\AI\asagake\ASAGAKE.xlsm`
  - `C:\AI\asagake\state\smtp.json` (if email sending is needed)

## Laptop environment setup (minimum)
1) Create venv + install deps:
   - `py -3.13 -m venv .venv`
   - `.\.venv\Scripts\activate`
   - `pip install -r requirements.txt`
   - `pip install -e .`
2) Excel setup:
   - Open `ASAGAKE.xlsm`, enable macros.
   - Run `SetupDashboardUIV2` once.

## How to sync latest changes on laptop
- No PR needed. Use Git pull:
  - `cd C:\AI\asagake`
  - `git pull --ff-only`
  - (In Cursor: Source Control → Pull, or run the same command in terminal.)


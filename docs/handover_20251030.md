# Handover Notes (2025-10-30) – Hypothesis Pipeline Validation

## 実施内容
- `scripts/bt_opt30_forward.py` の Hypothesis 計測をシングルティッカーでドライラン。`--analysis-ledger` と `--enable-market-features` を個別にテストし、`output/bt30/test_hypo_quick` と `output/bt30/test_hypo_market` に指標とレポートが正しく生成されることを確認。
- `scripts/run_post_nightly.cmd` を実行し、最新の NIGHTLY ラン (`output/bt30/NIGHTLY_20251029`) が `tools/analyze_param_stats.py` → `tools/update_ineffective_bands.py` まで問題なく回ることを確認。`reports/param_stats/NIGHTLY_20251029/` が再生成され、「No J values qualified for masking」と出力。
- 新しく取得し直した 1 分足 parquet をスポットチェック (`data/raw/yahoo_1m/1605.T/2025-09-04.parquet` など) し、期待どおりの OHLCV 構造と値で読み込めることを確認。
- Todo を更新し、Hypothesis 基盤の検証を進行中タスクとして明示。

## 次のステップ
1. 300 銘柄ユニバースで `scripts/nightly_build_candidates.py` を `--analysis-ledger` 付きで実行し、`hypothesis_trade_ledger.csv`／`hypothesis_summary.json` の内容をレビュー。
2. `data/raw/yahoo_1m/` 配下の parquet 全体について、欠損値やカラム欠落をチェックするスクリプトを走らせ、夜間ジョブに乗せる前に正常性を保証。
3. テスト結果を踏まえて、J 値や TP/SL の動的調整係数を設計し、必要に応じて GAP ルールを拡張。

## 備考
- Hypothesis 系の “メーター” は整備済みだが、係数は現状ノーチューニング。300 銘柄バッチで収集するデータを元に今後調整予定。
- parquet 再取得によりファイルサイズが大きく変わっているので、整合性チェックと差分管理を徹底すること。

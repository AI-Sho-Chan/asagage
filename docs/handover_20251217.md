# Handover 2025-12-17

## 1) ASAGAKE.xlsm（稼働中Excel）を“画面共有なしで”確認できるか

結論：**できます**。ExcelがこのPC上で起動していれば、COMで「開いているASAGAKE.xlsm」の**現在値**を読み取れます（画面そのものは見えないが、セルの値は読める）。

今回、稼働中の `C:\AI\asagake\ASAGAKE.xlsm` に接続して `NewDashboardV2` を確認したところ：

- 指数のセル（`B2/C2/E2/F2`）は数値が入っており、RSS接続は生きている状態でした。
- `J_th`（ヘッダ `J_th`、データ行6〜）は **NUM=17 / BAN=4 / ERR=0**（合計21行）でした。
- ただし、`B3`/`D3` のトレンド表示は依然として文字化けが残っていました（VBA側をASCII化する修正は入れてあるが、ブック側にまだ反映されていない可能性）。

## 2) Top200常連（永久保存対象）の1分足保存

目的：週末バッチのネット取得を減らし、**次回以降の時間短縮**と、90日・120日へ伸ばす“土台”を作る。

対応：

- `scripts/run_update_regulars_1m.ps1` を手動実行し、`data/universe/top_regulars_ever.csv`（Top200常連・永久保存対象）を使って minute_cache を深掘りしました。
- 生成されたパーケットを `cache/yahoo_1m_regulars` にミラーし、GCS の `gs://asagage-weekend-output/yahoo_1m_regulars` へ同期しました。

補足：このスクリプトは `gsutil` を ProcessStart で起動するため、環境によっては `gsutil.cmd` が見つからずアップロードが失敗することがありました。

## 3) 追加修正（運用の安定化）

- `scripts/run_update_regulars_1m.ps1` を修正し、`gsutil.cmd` を自動検出して使うようにしました（Scheduled Task でもアップロードが失敗しにくくなります）。

## 4) 次にやること（ユーザー側作業）

- `B3/D3` の文字化けを確実に解消するには、ブックのVBAモジュール `AutoTraderAdvanced` が最新になっている必要があります。
  - `excel/AutoTraderAdvanced.bas`（リポジトリ側）を再インポートし、`SetupDashboardUIV2` → `RefreshTrendsV2` を実行して表示を確認してください。


# Handover 2025-11-09

## 今日の変更点（要約）
- restore_dashboard_formulas.py の対象を ASAGAKE.xlsm に変更（バックアップ名も ASAGAKE_* に統一）。
- run_weekly_screening.ps1 の係数計算前に minute cache を事前取得（tools/update_minute_cache.py 呼び出しを正しく挿入）。
- tools/compute_dashboard_coeffs.py で YF 8日制限時や空DF時に 'ts' 欠損で落ちないようガードを追加。
- ランナーの流動性フィルタを --liquidity-quantile 0.3 に設定（ユニバース縮小の過剰抑制を緩和）。
- docs/codex.md に ASAGAKE 移行メモを追記（SHINSOKU 残存参照の扱い）。

## 発生していたエラーと対策
- compute_dashboard_coeffs: Yahoo 1m の「直近8日」制限越えで ^N225 欠損→ 'ts' KeyError。
  - 対処: 係数計算だけ --history-days 8 に短縮＋ 'ts' ガード。直前に update_minute_cache で銘柄の minute を確保。
- バッチ終盤で scripts/restore_dashboard_formulas.py 実行時に SHINSOKU.xlsm が開いてしまう。
  - 対処: 既定参照を ASAGAKE.xlsm に修正。

## 運用確認
- 現在、夜間/週末バッチの Python プロセスは稼働無し。最新 nightly_status.txt は完了ステータス（ただし returncode=1 は係数計算失敗の名残）。
- ASAGAKE.xlsm は修復スクリプト適用済み、VBAの再インポート完了（Excelは閉じた状態で実行）。

## 残タスク（優先順）
1) trend_bp_th A/B テスト（15/20/25/30、flat含む/除外）→ リプレイ集計と比較CSVを作成して報告。
2) j-cross BUY の TP/SL 細分化（AM0945/AM1015中心、AM1000 も復帰）→ グリッド拡張して夜間でスモーク実行。
3) BoardLogger の 09:00?15:30 常時収集タスク（5s間隔、保持30日）→ Task Scheduler へ登録、docs 追記。
4) 流動性判定のリファクタ（日次出来高で一次入場→セッション内ヒット0でも即除外しない）→ nightly_build_candidates.py を改修。
5) aggregate_candidates に勝率下限（>=0.60）を追加して影響評価→ 結果が良好なら本番化。
6) minute cache の更新順序と --universe-source 連携（ts 欠損を事前に解消）→ weekly/weekday 両方に適用。
7) AM1000 を週末プロファイルへ復帰、パラメータは BUY 特化で微調整。
8) スパイク（|J|>=3σ 等）の BLOCKED_SPIKE 処理とイベントログ化→ Excel/VBA＋analysis/j_spike_events.csv。
9) ラン完了後の自動分析（run_trade_analysis.ps1）を夜間/週末の後段で確実に実行→ 失敗時リトライを追加。

## 次アクション（バッチ非稼働時に実施）
- 上記1)?9)を順次実装→ スモーク→ 週末→夜間の順に再キック→ 変更前後比較をレポート化して共有。

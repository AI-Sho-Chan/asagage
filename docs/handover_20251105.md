# Handover Notes (2025-11-05)

## ASAGAKE V2 ダッシュボード（緊急対応・本番差替え）
- `scripts/build_dashboard_ui.py` を全面修正（Unicodeエスケープで日本語キャプション/ラベル/FormulaLocal挿入を安定化）。
- `excel/AutoTraderAdvanced.bas` の動的パラメータ参照を Row2（D2/E2/F2/G2/L2）に修正。
- `scripts/e2e_demo_on_copy.py` を強化：マクロ不可でもCOMのみでOrdersにデモ事前指値を記録（7203.T／数量可変）。
- 手順通り、`ASAGAKE.xlsm` をバックアップ作成後に `ASAGAKE_work_YYYYMMDD_HHMMSS.xlsm` で置換完了。
- ダッシュボード：
  - 行2にパラメータ（NKY_Code/NKY_Last/NKY_ChgPct/Bias_bp/...）を設置。NKY_Last/ChgPctはRSSの`現在値`/`騰落率`で自動更新（FormulaLocal）。
  - 行4に日本語項目名、行5に要求順の英字ヘッダー（Ticker/Name/…）。
  - ボタン（デモ開始/停止・本番開始/停止・候補銘柄取込）はセル4列×高さ2行で配置。
  - PrevClose/VWAP/BestBid/BestAsk/Name は RSS `前日終値`/`VWAP`/`最良買気配値`/`最良売気配値`/`銘柄名` を注入。
- Orders 確認：コピー上でデモ前置注文を記録（合計4行）。本番ファイルへ反映済み。

注記:
- マクロ実行は環境のセキュリティ設定に依存するため、呼び出し失敗時はスクリプト側がフォールバックで処理します（UI構築/Orders記録）。
- 以後の恒久対応として、NKYバイアス→J_th補正の完全自動連動は、VBA `ApplyDynamicSignalsV2`（Row2参照）で提供。Excelのマクロ実行が有効であれば即時反映されます。

## What changed (A+B+C)
- A: 候補集計のCI下限をパラメタ化（デフォルト0.65）
  - `scripts/nightly_build_candidates.py` に `--min-forward-ci` を追加、集計ロジックで `forward_win_ci_low >= min_forward_ci` を適用。
  - `scripts/run_nightly_candidates.ps1` は `--min-forward-ci 0.65` を付与。
- B: 週次スクリーニング導入
  - `scripts/run_weekly_screening.ps1`: 金曜16:30想定。Universe=Yahoo top 300、coarse+refine（軽量）、集計へ。
  - `tools/aggregate_weekly_candidates.py`: 勝率>=0.70、PF_eff>=1.30、exp_bp>0、trades>=5 を満たす銘柄から score で上位（目安50）抽出。出力は `output/excel/weekly_candidates_YYYYMMDD.csv`。
- C: ダッシュボードV2/VBA高度化の雛形
  - `excel/AutoTraderAdvanced.bas`: NewDashboardV2 用パラメタ（ギャップ/バイアス/相関/トレール/予算/事前指値閾値）と、
    動的J_th、事前指値エントリ、TP/SL/トレール、サイズ計画（ログは Orders）を実装する雛形。
  - `scripts/excel_install_advanced.py`: 既存 `SHINSOKU.xlsm` へ COM 経由インポート（事前バックアップ作成）。

## How to use / Next steps
1) 夜間（平日）
   - これまで通り `scripts/run_nightly_candidates.ps1` 実行。`--min-forward-ci 0.65` で集計されます。
2) 週次（金曜16:30）
   - `scripts/run_weekly_screening.ps1` をタスクスケジューラへ登録。完了後 `output/excel/weekly_candidates_YYYYMMDD.csv` を候補に。
3) ダッシュボードV2 準備（Excelは閉じた状態で）
   - `python scripts/excel_install_advanced.py --excel C:/AI/asagake/SHINSOKU.xlsm`
   - Excel起動後、`AutoTraderAdvanced.SetupNewDashboardV2` → `InstallRealtimeFormulasV2` → `ApplyDynamicSignalsV2` を順に実行。
   - 事前指値のログ確認は `Orders` シート。

## Notes / Constraints
- 既存の NewDashboard には書き込みを行っていません。V2 は新シートで分離実装。
- Rakuten 連携の実発注は、RSS関数（`RssOrder` 系）との統合箇所に依存。現時点はドライラン・ログ（Orders）を優先。
- 追加のギャップバケット別パラメタ最適化は、btスクリプト側に拡張余地あり。

## Suggested follow-ups
- V2 に `RssOrder` 呼び出し（実発注）を段階導入し、`AutoTrader.PlaceOrderDryRun` との二重記録をやめる。
- 週次ユニバースの相関（NKY）プリセットを生成する小スクリプト追加。
- 事前指値の資金配分アルゴ（気配数量×予算上限×銘柄スコア）を確定版へ強化。

# Handover Notes (2025-11-08)

## Dashboard Header Fix
- `scripts/repair_asagake_dashboard.py` の `JP_MAP` で BatchKind / NKY_* / J_ratio ラベルが文字化けしていたため、UTF-8 の正しい表記に修正。
- Excel を閉じた状態で `python scripts/repair_asagake_dashboard.py --excel C:/AI/asagake/ASAGAKE.xlsm` を実行し、`ASAGAKE_backup_20251108_073740.xlsm` を取得したうえで本番ファイルを再構築。
- `python scripts/excel_install_macros.py C:/AI/asagake/ASAGAKE.xlsm excel/AutoTraderAdvanced.bas excel/cDashboardWatcher.cls` を再実行して VBA モジュールをクリーンに再導入。
- `NewDashboardV2` の AP～AT 行（Batch種別 / NKY日次トレンド / NKY窓トレンド / NKY許容サイド / J到達率）が正しい日本語ラベルへ更新済み（`sharedStrings.xml` で `\u7a2e` 等を確認）。
- COM 経由で VBA プロジェクトを確認し、`AutoTraderAdvanced` と `cDashboardWatcher` のみが残っていることを確認（旧 AutoTrader モジュールは存在せず）。

## VBA Compile Error Fix
- Excel 起動時に「修正を終了: 識別子」が発生していた原因は、`excel/AutoTraderAdvanced.bas` のモジュール先頭で `Private Dim` と誤記していたため。`Private gDashboardWatcher As ...` など 13 個の宣言を `Private` のみに修正。
- 修正後は必ず `python scripts/excel_install_macros.py C:/AI/asagake/ASAGAKE.xlsm excel/AutoTraderAdvanced.bas excel/cDashboardWatcher.cls` で再インポートする。Excel/VBE から直接編集しない。
- 再インポート後、Excel を開き `Debug > Compile VBAProject` を実行してノーエラーを確認済み。コンパイルエラー再発時は同ファイルを確認し、`Private` 宣言へ戻す。

## トレンドドライバ / 方向フィルタ実装
- `scripts/research_trend_filters.py` を追加。直近30営業日・上位100銘柄（AM15/AM0930/PM1）のトレードを分析し、`analysis/trend_alignment_summary.csv` などを自動生成。NKY窓連動で +44.8bp、TOPIX窓連動で +41bp を確認。
- 同スクリプトは `--output-dir analysis/trend90 --lookback-days 90 --top-n 300` でも実行し、現状サンプルが 1,053 件のままであることを確認（追加データ取り込み後に再実行することで自動的に更新される）。
- `scripts/nightly_build_candidates.py` に `apply_trend_preferences` を追加し、`analysis/trend_ticker_preference.csv` から銘柄ごとの `trend_driver / trend_window / trend_bp_th / trend_allowed_policy` を候補CSVへ書き戻す。
- ダッシュボード列（`trend_*`）を `scripts/repair_asagake_dashboard.py` に追加。J_th 式は `trend_allowed_policy=ALIGNED_ONLY` かつ `NKY_allowed_side` と `EntrySide` が逆行した場合に自動で `BAN` を返すように更新。さらに `J_ratio` の条件付き書式（0.8以上=薄緑、1.0以上=濃緑）と、逆行中の行を薄グレー＋灰色フォントで表示する行全体の書式を設定。
- `excel/AutoTraderAdvanced.bas` の `ImportCandidatesV2` が新列（trend_driver/trend_window/trend_bp_th/trend_allowed_policy）を取り込み・クリアできるよう拡張済み。

## BoardLogger / 5秒Jログ
- `scripts/board_snapshot.py` に `snapshot_dashboard_j` を追加し、`NewDashboardV2` の `Ticker/J/J_th/J_ratio/EntrySide/Selected/NKY_allowed_side/trend_*` を CSV へ書き出す機能を実装。
- `scripts/board_logger_daemon.py` は `--dashboard`（デフォルト `C:/AI/asagake/ASAGAKE.xlsm`）と `--dash-outdir output/j_logs` を受け取り、従来の板ログと同周期で `dashboard_j_<timestamp>.csv` も保存するよう変更。`--interval` の下限は 5 秒に緩和済み。

## 運用ドキュメント
- `docs/codex.md` に「JP_MAP が mojibake のままだと修正が反映されない」点と、repair→macro install をセットで実行する注意書きを追記。
- 次回以降も Excel/VBE を開かずに同じ手順を踏めるよう、貼付エラー発生時の復旧フローを上記ドキュメントへ明記。

---

## 2025-11-08 Automation Follow-up
- `scripts/run_trade_analysis.ps1` の終端で `scripts/build_j_stats.py --rules state/strategy_rules.ini` を常時実行。これにより `state/j_stats.csv`（銘柄×セッション別の J 到達率 μ/σ）と `bb_flat_k` / `bb_trend_k` が毎晩自動更新され、`AutoTraderAdvanced.ApplyDynamicSignalsV2` のボリンジャーブロックに即反映される。
- 金曜の `run_trade_analysis.ps1` は追加で `scripts/research_trend_filters.py --lookback-days 90 --top-n 300 --sessions AM15,AM0930,AM0945,AM1000,AM1015,AM1030,PM1` を実行し、指数ドライバの相関マップを週1で最新化する。
- `scripts/board_logger_daemon.py` に `--dash-outdir output/j_logs --interval 5 --retain-days 30` を追加し、タスクスケジューラから平日 08:55 に `run_board_logger_daemon.cmd` を起動。5秒間隔で板ログ＋Jログが蓄積され、30日より古いフォルダは自動で削除される。
- �T��/�i�C�g�̃o�b�`���ԒZ�k�ɔ����Anightly_build_candidates.py �� plan-profile ��ǉ����A�T���� AM15/0930/0945/1015/1030�~{j-only,j-cross}�A������ AM15/0930/0945/1000/1015/1030/PM1 ��]������\���֕ύX�BAM1000/PM1 �̏ڍגT���̓i�C�g���ŒS�ۂ���B
- bt_opt30_forward.py �� run_type/BT30_PARAM_PROFILE �ɉ������O���b�h�ؑւ������B�T��: ATR_n {2,3}, J_th {0.8,1.0,1.2,1.4,1.6,1.8,2.0,2.2,2.4,2.6,2.8,3.0}�ATPk {1.0,1.5,2.0,3.0}�ASLk {1.0,1.5,2.0}�B����: ATR_n {2}�AJ_th {0.8,1.0,1.2,1.4,1.6,1.8,2.0}�ATPk {1.5,2.0}�ASLk {1.0,1.5}�B
- run_weekend_then_nightly.ps1 / run_nightly_candidates.ps1 �� plan-profile�Euniverse-size�EBayesTrials ��؂�ւ��A�T��=���j�o�[�X200/Bayes20/timeout600s�A����=���j�o�[�X150/Bayes10/timeout300s�BGapBanPct=3%�EMinGap20bp(j-cross SELL)�ETrail_per_J=0.10 ������L�B
## 週末/ナイトバッチ再設計メモ (2025-11-09)
- 既存の長時間ジョブは一旦停止し、plan-profile=weekend（AM15/0930/0945/1015/1030×{j-only,j-cross}+RD窓）のみを優先実行。PM帯とAM1000はplan-profile=weekday（AM15/0930/0945/1000/1015/1030/PM1）へ移し、ナイトバッチで補完する。
- BT30_PARAM_PROFILEで週末/平日ごとのパラメータ・グリッドを切替。週末はATR_n {2,3}+J_th 0.8～3.0(0.2刻み)+TPk {1.0,1.5,2.0,3.0}+SLk {1.0,1.5,2.0}、平日はATR_n {2}+J_th ≤2.0+TPk {1.5,2.0}+SLk {1.0,1.5}。run_weekend_then_nightly.ps1／run_weekly_screening.ps1 から--plan-profileを渡し、bt_opt30_forward.pyはrun_type/環境変数で自動判定。
- j-cross SELLはstate/strategy_rules.iniのjcross_sell_require_nky_down=1・jcross_sell_min_gap_bp=20を既定値にし、ApplyDynamicSignalsV2/夜間バッチでNKY_allowed_sideがSELLを許容＋ギャップ20bp以上のケースのみ指値が生きるよう統合。週末バッチ探索でもGapBanPct=3%・MinGap=20bp(j-cross SELL時)を固定して低収益帯を初手で除外。
- 週末BayesTrials=20/timeout=600s・ユニバース200、ナイトBayesTrials=10/timeout=300s・ユニバース150に圧縮。長時間化した際はKill-OldRuns→パラメータ絞り込みで再実行する手順を標準化し、logs/sequential_runner.logでKill→再起動履歴を確認できるようにした。
- scripts/run_trade_analysis.ps1 完了後に scripts/research_trend_filters.py --lookback-days 90 --top-n 300 --sessions AM15,AM0930,AM0945,AM1000,AM1015,AM1030,PM1 を金曜のみ自動実行。結果は analysis/trend_ticker_preference.csv / analysis/trend_driver_overview.json を更新し、翌週の apply_trend_preferences が即反映される。
- board_logger_daemon.py --interval 5 --retain-days 30 を常駐させ output/j_logs/YYYYMMDD/dashboard_j_*.csv を自動蓄積。scripts/build_j_stats.py --rules state/strategy_rules.ini --logs output/j_logs を週末バッチの後段に組み込み、state/j_stats.csv を自動再生成→Excel側は ApplyDynamicSignalsV2 で即読込。
## 2025-11-09 Driver Filter & Jログ連携
- NKY に加え `TOPIX_Code/Last/ChgPct` と `TOPIX_TrendDay/TrendWindow/AllowedSide` を Param 行に追加。`repair_asagake_dashboard.py` が自動で RssIndexMarket フォーミュラを入れるため、Excel を開かずに RSS の初期設定が整います。
- ダッシュボードには `driver_day_trend` / `driver_window_trend` / `driver_allowed_side` 列を追加し、`AutoTraderAdvanced.ApplyDynamicSignalsV2` が銘柄ごとの `trend_driver` に応じた方向・許容サイドを毎サイクル書き戻します。BAN 判定と条件付き書式もこの列を参照するよう更新したため、NKY 以外のドライバでも逆行行が自動でグレーアウトします。
- VBA 側は `UpdateAllDriverTrends` → `UpdateDriverTrend` の仕組みに刷新。ドライバごとにセッションオープン・15本履歴を持ち、方向が変わったドライバだけ `CancelOppositeOrders` を呼ぶようにしました。`trend_driver` が空なら従来通り NKY が既定値です。
- `board_snapshot.py` で `session` と `driver_*` 列を CSV に出力し、`board_logger_daemon.py` が5秒おきに `output/j_logs/YYYYMMDD/dashboard_j_*.csv` を蓄積します。
- `scripts/build_j_stats.py` は `--logs output/j_logs --log-days 30` を入力に取り、トレードレジャーと J ログを結合して μ/σ を再推定。結果を `state/j_stats.csv` に書き戻した後、`trend_label` 列を使って `bb_flat_k` / `bb_trend_k` を `state/strategy_rules.ini` へ自動反映します。`scripts/run_trade_analysis.ps1` から新オプション付きで呼ぶため、週次/夜間バッチ完了後は J ボリンジャー閾値が自動で最新化されます。
- `board_logger` のローテーション (`retain-days=30`) と `build_j_stats.py --log-days 30` の窓が揃っているので、ファイルが肥大化しても自動で整理されます。古いログを削除してから再集計したい場合は `output/j_logs` を空にして再実行してください。
## 2025-11-09 Batch & Strategy Updates
- Liquidityフィルタの緩和方針を追加。`--liquidity-quantile` を0.3に変更し、日次出来高の上位70%を対象にしてからセッション内で `is_entry` を付与する（特別気配などで一時的に値が付かない銘柄も後続時間で評価できるようにする）。
- 日次出来高で一次フィルタ→セッション内では取引ゼロでも即除外しないロジックに切り替える残タスクを登録。バッチ完了後に `apply_liquidity_and_mark_entry` と関連処理を更新する。
- BoardLogger 常駐タスクを 08:55〜15:35（5秒間隔）で動かし、全時間帯のJログを `output/j_logs` に蓄積。`build_j_stats.py --logs` の再推定で AM0930 以降の BB閾値を得る計画。
- AM1000 セッションを週末 plan-profile に復帰させる（バッチ完了後に `nightly_build_candidates.py` の `plan_profile=weekend` に追加）。
- trend_bp_th 15/20/25/30bp のA/Bテスト、j-cross BUY グリッド0.05刻み、スパイク検知+BLOCKED_SPIKE、勝率下限60%導入、TOPIX銘柄比率レポートを残タスクとして明記。バッチ完了後に順次着手する。
- SELL運用: `jcross_sell_require_nky_down=1` と `jcross_sell_min_gap_bp=20` を維持し、夜間バッチのみSELLを許容する方針を再確認（週末AM15 SELLは `weekly_sell=disable` で除外）。
- 残タスク実行後（trend_bp_th A/B、j-cross BUY細分化、liq=0.3等）に週末→ナイトバッチを新仕様で再走させ、変更前（2025-11-09ラン）と比較したパフォーマンス分析レポートを作成。PF/勝率/取引数/方向別/セッション別の差分をまとめ、インサイトと次の改善提案を報告する。
## 2025-11-09 Performance Tasks (Post‑batch)
- Local optimizer pack（ソフトのみ・互換維持）
  - I/OをParquet/Featherへ切替（Raw/中間/_SUMMARY_系）。CSV入出力は互換維持のまま最終成果物のみ。
  - ハッシュキャッシュ（_HASH）と `--use-local-raw` を強制活用、再計算スキップ率を上げる。
  - `--jobs` を物理コア基準で自動最適化（現構成: 8–12 を推奨）。
  - Polars/Numba をホットスポットに段階導入（フラグ駆動でオン/オフ可能）。
  - フェーズ別の計測ログを StatusTracker に追加（I/O/計算/API の所要時間をJSON出力）。
- NVMe増設（ハード）
  - 中間/一時ファイル用に第2のNVMe（Gen4以上, 1–2TB）を追加。OS/ソースと分離しI/O競合を低減。
  - 手順: M.2スロット確認→装着→ディスクの初期化/フォーマット→`tools/update_minute_cache.py` の出力先・TEMPを新ドライブに変更。
  - 初心者でも可能（静電気対策・取付ねじ）。RAID0は要注意（バックアップ必須）。
- クラウドHPC二段構え
  - 週末のフル探索はクラウド（例: AWS c7i.48xlarge など）に寄せ、夜間の軽い微調整はローカルで実行。
  - 付帯物: Dockerfile/Ray起動スクリプト/`compose-ray.yaml`、S3バケット（analysis/outputの保存）、認証はSSM/KeyVault。
  - 変更は “実行基盤” のみで、ASAGAKE.xlsm とロジックはそのまま（壊さない）。
- 比較レポート
  - 上記の最適化適用後に週末→ナイトを新仕様で再実行し、11/09 ランとの差分（PF/勝率/取引数/方向/セッション）を報告。

# Handover Notes (2025-11-08)

## Dashboard Header Fix
- `scripts/repair_asagake_dashboard.py` の `JP_MAP` で BatchKind / NKY_* / J_ratio ラベルが文字化けしていたため、UTF-8 の正しい表記に修正。
- Excel を閉じた状態で `python scripts/repair_asagake_dashboard.py --excel C:/AI/asagake/ASAGAKE.xlsm` を実行し、`ASAGAKE_backup_20251108_073740.xlsm` を取得したうえで本番ファイルを再構築。
- `python scripts/excel_install_macros.py C:/AI/asagake/ASAGAKE.xlsm excel/AutoTraderAdvanced.bas excel/cDashboardWatcher.cls` を再実行して VBA モジュールをクリーンに再導入。
- `NewDashboardV2` の AP～AT 行（Batch種別 / NKY日次トレンド / NKY窓トレンド / NKY許容サイド / J到達率）が正しい日本語ラベルへ更新済み（`sharedStrings.xml` で `\u7a2e` 等を確認）。
- COM 経由で VBA プロジェクトを確認し、`AutoTraderAdvanced` と `cDashboardWatcher` のみが残っていることを確認（旧 AutoTrader モジュールは存在せず）。

## VBA Compile Error Fix
- Excel 起動時に「修正を終了: 識別子」が発生していた原因は、`excel/AutoTraderAdvanced.bas` のモジュール先頭で `Private Dim` と誤記していたため。`Private gDashboardWatcher As ...` など 13 個の宣言を `Private` のみに修正。
- 修正後は必ず `python scripts/excel_install_macros.py C:/AI/asagake/ASAGAKE.xlsm excel/AutoTraderAdvanced.bas excel/cDashboardWatcher.cls` で再インポートする。Excel/VBE から直接編集しない。
- 再インポート後、Excel を開き `Debug > Compile VBAProject` を実行してノーエラーを確認済み。コンパイルエラー再発時は同ファイルを確認し、`Private` 宣言へ戻す。

## トレンドドライバ / 方向フィルタ実装
- `scripts/research_trend_filters.py` を追加。直近30営業日・上位100銘柄（AM15/AM0930/PM1）のトレードを分析し、`analysis/trend_alignment_summary.csv` などを自動生成。NKY窓連動で +44.8bp、TOPIX窓連動で +41bp を確認。
- 同スクリプトは `--output-dir analysis/trend90 --lookback-days 90 --top-n 300` でも実行し、現状サンプルが 1,053 件のままであることを確認（追加データ取り込み後に再実行することで自動的に更新される）。
- `scripts/nightly_build_candidates.py` に `apply_trend_preferences` を追加し、`analysis/trend_ticker_preference.csv` から銘柄ごとの `trend_driver / trend_window / trend_bp_th / trend_allowed_policy` を候補CSVへ書き戻す。
- ダッシュボード列（`trend_*`）を `scripts/repair_asagake_dashboard.py` に追加。J_th 式は `trend_allowed_policy=ALIGNED_ONLY` かつ `NKY_allowed_side` と `EntrySide` が逆行した場合に自動で `BAN` を返すように更新。さらに `J_ratio` の条件付き書式（0.8以上=薄緑、1.0以上=濃緑）と、逆行中の行を薄グレー＋灰色フォントで表示する行全体の書式を設定。
- `excel/AutoTraderAdvanced.bas` の `ImportCandidatesV2` が新列（trend_driver/trend_window/trend_bp_th/trend_allowed_policy）を取り込み・クリアできるよう拡張済み。

## BoardLogger / 5秒Jログ
- `scripts/board_snapshot.py` に `snapshot_dashboard_j` を追加し、`NewDashboardV2` の `Ticker/J/J_th/J_ratio/EntrySide/Selected/NKY_allowed_side/trend_*` を CSV へ書き出す機能を実装。
- `scripts/board_logger_daemon.py` は `--dashboard`（デフォルト `C:/AI/asagake/ASAGAKE.xlsm`）と `--dash-outdir output/j_logs` を受け取り、従来の板ログと同周期で `dashboard_j_<timestamp>.csv` も保存するよう変更。`--interval` の下限は 5 秒に緩和済み。

## 運用ドキュメント
- `docs/codex.md` に「JP_MAP が mojibake のままだと修正が反映されない」点と、repair→macro install をセットで実行する注意書きを追記。
- 次回以降も Excel/VBE を開かずに同じ手順を踏めるよう、貼付エラー発生時の復旧フローを上記ドキュメントへ明記。

# Handover 2025-12-26

## Summary
- Anchored CorrNKY/CorrTOPIX and dashboard slope coefficients to `target_date` to avoid A/B drift.
- Added a lightweight tool to refresh candidates CSVs without rerunning weekend WF.
- Planned VM-side refresh of `candidates_nextday` (A/B) for 2025-12-19 using the new tool.

## Changes
- `tools/compute_dashboard_coeffs.py`
  - Added `--end-date` to fix coefficient history to a specific date.
- `scripts/nightly_build_candidates.py`
  - Corr map now anchors to `target_date` and is reused in CSV enrichment.
  - Added `--corr-history-days` (default 180 days).
  - Pass `--end-date` to `compute_dashboard_coeffs`.
- `tools/refresh_candidate_coeffs.py`
  - New: refresh Bias/Corr slopes + CorrNKY/CorrTOPIX for existing candidates CSVs without rerunning WF.

## VM action (needed)
- After pull, run:
  - `python tools/refresh_candidate_coeffs.py --candidates output/excel/candidates_nextday.csv --end-date 20251219`
  - `python tools/refresh_candidate_coeffs.py --candidates output/excel/candidates_nextday_B_coarse3.csv --end-date 20251219`
  - This makes A/B comparable without rerunning the long weekend batch.

## Notes
- A/B candidates were identical except for `BiasSlope_row`, `CorrSlope_row`, `CorrNKY`, `CorrTOPIX` caused by date drift.
- After refresh, those differences should disappear; then B can replace A safely.
